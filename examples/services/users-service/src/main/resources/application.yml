spring:
  application:
    name: users-service
  kafka:
    bootstrap-servers: kafka:9092
server:
  port: 8081
  servlet:
    context-path: /v1
orders:
  base-url: http://orders-service/api
  topic: orders.created
users:
  topic: users.updated
workflow:
  engine-url: http://bpmn-engine:8090
---
spring:
  config:
    activate:
      on-profile: local
orders:
  base-url: http://localhost:8082/api
